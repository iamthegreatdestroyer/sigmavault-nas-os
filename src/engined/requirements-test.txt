# Minimal test dependencies for CI
# Heavy ML dependencies (torch, transformers, liboqs) are optional for testing

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-benchmark>=4.0.0
hypothesis>=6.100.0

# Core runtime (light dependencies only)
aiohttp>=3.9.0
structlog>=24.0.0
prometheus-client>=0.20.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# API framework (required for imports)
fastapi>=0.109.0
starlette>=0.35.0
httpx>=0.27.0

# System monitoring
psutil>=5.9.0

# Compression (pure Python/C bindings, install quickly)
zstandard>=0.22.0
lz4>=4.3.0
brotli>=1.1.0

# gRPC
grpcio>=1.68.0
grpcio-tools>=1.68.0
protobuf>=5.29.0

# Async
anyio>=4.7.0

# Numpy 2.x (matches pyproject.toml)
numpy>=2.0.0
